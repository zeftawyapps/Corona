<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.z_apps.Helteycare">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon"
        android:supportsRtl="true"
        android:theme="@style/AppThemenoaction">
        <activity android:name=".Project.Activity.Act_splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Project.Act_Web" />
        <activity
            android:name=".Project.Activity.Act_pationDetails"
            android:theme="@style/AppTheme" />
        <activity android:name=".Project.Activity.Act_Doctor" />
        <activity
            android:name=".Project.Activity.Act_userjob"
            android:theme="@style/AppTheme" />
        <activity android:name=".Project.Act_AddDataForTEst" />
        <activity android:name=".Project.Activity.Act_setting" />
        <activity
            android:name=".Project.Activity.Act_Mainscreen"
            android:theme="@style/AppTheme_main">
        </activity>
        <activity
            android:name=".Project.Main2Activity"
            android:label="@string/title_activity_main2"
            android:theme="@style/AppThemenoaction.NoActionBar" />
        <activity
            android:name=".Project.Act_Mainscreen"
            android:label="@string/title_activity_act__mainscreen"
            android:theme="@style/AppThemenoaction.NoActionBar" />
        <activity android:name=".Project.Activity.Act_virifay" />
        <activity android:name=".Project.Activity.Act_Question" />
        <activity android:name=".Project.Activity.Act_login" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".Project.MapsActivity"
            android:label="@string/google_maps_key" />
        <activity android:name=".Project.Activity.Act_signUp" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity android:name=".Project.Activity.MainActivity" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE)" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>